<Window x:Class="MedicalReferral.AddReferralWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Referral" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox x:Name="PatientNameTextBox" ToolTip="Enter Patient Name" Margin="5" PlaceholderText="Patient Name"/>
            <TextBox x:Name="DoctorNameTextBox" ToolTip="Enter Doctor Name" Margin="5" PlaceholderText="Doctor Name"/>
            <TextBox x:Name="ExaminationTypeTextBox" ToolTip="Enter Examination Type" Margin="5" PlaceholderText="Examination Type"/>
            <DatePicker x:Name="DatePicker" ToolTip="Select Date" Margin="5"/>
            <Button Content="Save" Click="Save_Click" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
